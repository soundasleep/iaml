Migrator 0.5 to 0.6: values elements can no longer be used directly with DataFlowEdges, and must be migrated manually. (<properties>)
Migrator 0.5 to 0.6: results elements can no longer be used directly with DataFlowEdges, and must be migrated manually. (<results>)
Migrator 0.5 to 0.6: currentPointer elements can no longer be used directly with DataFlowEdges, and must be migrated manually. (<currentPointer>)
Migrator 0.5 to 0.6: fieldValue elements can no longer be used directly with DataFlowEdges, and must be migrated manually. (<fieldValue>)
Migrator 0.5 to 0.6: currentInput elements can no longer be used directly with DataFlowEdges, and must be migrated manually. (<currentInput>)
Migrator 0.5 to 0.6: values elements can no longer be used directly with DataFlowEdges, and must be migrated manually. (<properties>)
Migrator 0.5 to 0.6: values elements can no longer be used directly with DataFlowEdges, and must be migrated manually. (<properties>)
Migrator 0.5 to 0.6: values elements can no longer be used directly with DataFlowEdges, and must be migrated manually. (<properties>)
Migrator 0.5 to 0.6: DomainAttributes can no longer contain Operations directly. (<operations>)
Migrator 0.5 to 0.6: DomainAttributes can no longer contain Conditions directly. (<conditions>)
Migrator 0.5 to 0.6: DomainAttributes can no longer contain Conditions directly. (<conditions>)
Migrator 0.5 to 0.6: DomainAttributes can no longer contain onChange directly. (<onChange>)
Migrator 0.5 to 0.6: DomainAttributes can no longer contain fieldValue directly. (<fieldValue>)
Migrator 0.5 to 0.6: DomainAttributes can no longer contain Operations directly. (<operations>)
Migrator 0.5 to 0.6: DomainAttributes can no longer contain Conditions directly. (<conditions>)
Migrator 0.5 to 0.6: DomainAttributes can no longer contain Conditions directly. (<conditions>)
Migrator 0.5 to 0.6: DomainAttributes can no longer contain onChange directly. (<onChange>)
Migrator 0.5 to 0.6: DomainAttributes can no longer contain fieldValue directly. (<fieldValue>)
Migrator 0.5 to 0.6: DomainAttributes can no longer contain Operations directly. (<operations>)
Migrator 0.5 to 0.6: DomainAttributes can no longer contain Conditions directly. (<conditions>)
Migrator 0.5 to 0.6: DomainAttributes can no longer contain Conditions directly. (<conditions>)
Migrator 0.5 to 0.6: DomainAttributes can no longer contain onChange directly. (<onChange>)
Migrator 0.5 to 0.6: DomainAttributes can no longer contain fieldValue directly. (<fieldValue>)
