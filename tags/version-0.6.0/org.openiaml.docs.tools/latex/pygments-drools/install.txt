How to install the Drools mapping.

First, copy to your /pygments/lexers folder.

Then install the mappings:

  $ python _mapping.py

You should then see 'drools, drl' listed:

  $ pygmentize -L lexers
