How to install the OCL mapping.

First, copy to your /pygments/lexers folder.

Then install the mappings:

  $ python _mapping.py

You should then see the lexer listed:

  $ pygmentize -L lexers
