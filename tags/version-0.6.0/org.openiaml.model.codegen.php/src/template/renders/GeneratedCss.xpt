«IMPORT iaml»

«EXTENSION template::GeneratorExtensions»

/**
 * Define any additional CSS for this page.
 */
«DEFINE generatedCSS FOR model::visual::Frame-»
«FOREACH eAllContents.typeSelect(model::visual::InputForm) AS form»
	«FOREACH form.children.typeSelect(model::visual::IteratorList) AS list»
		«IF !list.generatedBy.typeSelect(model::wires::AutocompleteWire).isEmpty»
			/* list '«safeName(list)»' was generated by an AutocompleteWire */
			
			/* form */
			#«safeName(form)» { position: relative; }
			
			/* autocomplete table */
			#«safeName(list)» {
				border: 1px solid #666;
				box-shadow: .2em .2em #ccc;
				-moz-box-shadow: .2em .2em .2em #ccc;
				width: 60%;
				position: absolute;
				top: 4.8em;
				left: 4em;
			}
			
			#«safeName(list)» thead {
				display: none;
			}
			
			#«safeName(list)» td {
				border: 0;
				padding: 3px;
				margin: 3px;
				cursor: pointer;
			}
			
			#«safeName(list)» td label {
				cursor: pointer;
			}
			
			#«safeName(list)» tr {
				background: #ddf;
			}
			
			#«safeName(list)» tr:hover {
				background: #bbe;
			}
		«ENDIF»
	«ENDFOREACH»
«ENDFOREACH»
«ENDDEFINE»
