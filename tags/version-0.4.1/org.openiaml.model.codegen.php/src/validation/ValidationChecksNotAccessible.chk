import iaml;

// NOTE: checks cannot have a semicolon in the exception, otherwise the 
// OAW parser won't be able to parse beyond it
extension validation::NotAccessible;

context model::visual::Page ERROR "Not accessible from Home page":
	accessible(this);
