How to install the Alloy mapping.

First, copy to your /pygments/lexers folder.

Then install the mappings:

  $ python _mapping.py

You should then see 'alloy, als' listed:

  $ pygmentize -L lexers
